{
  "name": "translation-pms-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "start": "node backend/src/server.js"
  }
}
