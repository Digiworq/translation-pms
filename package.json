{
  "name": "translation-pms",
  "version": "1.0.0",
  "description": "Translation & Localization Project Management System Root Package",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "seed:mysql": "cd backend && node src/seedMysqlNow.js",
    "clean:mysql": "cd backend && node src/cleanMysql.js",
    "prisma:generate": "cd backend && npx prisma generate",
    "prisma:migrate": "cd backend && npx prisma migrate dev --name init",
    "prisma:seed": "cd backend && npm run prisma:seed",
    "prisma:studio": "cd backend && npx prisma studio"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
